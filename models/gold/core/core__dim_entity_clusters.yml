version: 2

models:
  - name: core__dim_entity_clusters
    description: "{{ doc('core__dim_entity_clusters') }}"

    columns:
      - name: ADDRESS
        description: "{{ doc('address') }}"
        tests:
          - unique
          - not_null
      - name: ADDRESS_GROUP
        description: "{{ doc('address_group') }}"
        tests:
          - not_null
      - name: PROJECT_NAME
        description: "{{ doc('project_name') }}" 
